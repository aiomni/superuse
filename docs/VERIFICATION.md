# 验证记录

环境：2026-09-22，macOS 27.0，Xcode 27.0，Swift 6.4，Apple Silicon。

## 文字 Pin 直接编辑（2026-09-22）

- 文字 Pin 可直接输入、选择、剪切、纯文本粘贴和清空，修改即时保留在当前会话快照中，不回写历史条目；复制使用编辑后的文字。每个窗口有独立撤销 / 重做历史，状态同步保留光标与输入法组合状态。
- `swift test --filter PinTests`：18 项通过。新增覆盖 Unicode / emoji 编辑、复制最新内容、原生撤销重做、满窗口数下替换资源占用、清空为零占用、超预算粘贴拒绝、其他 Pin 占用预算后的撤销、输入法组合状态，以及原历史独立性。
- 本轮完整 `swift test` 中 25 个核心测试通过；47 个 AppKit 测试中 Pin、布局和其他集成测试通过，截图放大镜的 `loupeFitsAtScreenEdgesAndAcrossAppearancesWithoutDimmingFullScreen` 离屏颜色断言失败。该测试单独运行同样失败；不将整套验证记为通过。
- 独立原生预览使用专用剪贴板和合成文字，实机完成「36 → 44 → 撤销回 36 → 重做到 44」。复制编辑后的整段文字，再临时改成 60 后全选粘贴，确认恢复为 44，验证实际复制的是当前内容。真实中文输入法候选交互仍需手工补充验收；自动测试通过 `setMarkedText` 覆盖组合状态。
- `./scripts/build-app.sh release` 成功；新 arm64 应用包的 Info.plist、严格签名和既有证书绑定 requirement 检查通过。

## Pin 验证（2026-09-22）

- 本次 `swift test` 通过：22 个核心测试、39 个 AppKit / 集成测试，共 61 项；参数化测试另外覆盖多个输入组合。没有编译警告。
- Pin 回归覆盖截图成品标注和原始像素尺寸、Pin 不改写剪贴板、失败时保留编辑、剪贴板所选快照、编辑 / 淘汰与主动删除的不同生命周期、缩放 / 不透明度不影响复制、文字选中复制、穿透恢复和窗口关闭。测试全部使用专用剪贴板及临时持久化路径，Pin 窗口不在桌面显示。
- 资源检查覆盖数量和内存预算、无效图片、超大尺寸、截图裁剪不保留整屏父位图，以及菜单不持有已关闭图片。关闭后的释放检查在 AppKit autorelease pool 排空后执行；退出清理同时清除未完成的截图抑制 token。
- 截图抑制覆盖嵌套 token、重复恢复、原有隐藏状态、截图期间新建 / 关闭 Pin，以及截图任务在实际屏幕读取前取消。小选区状态提示的回归发现并修复了新增 Pin 按钮后的工具栏重叠；两条工具栏锚点仍保持稳定。
- `InterfaceLayoutTests` 渲染了亮色、暗色和两种高对比度外观；检查 Pin 的最小尺寸、长文本、图片居中和缩放后的留白，以及新增入口后的工具箱、设置和剪贴板布局。Pin 使用 Preview 风格的原生标题栏和工具栏，隐藏重复标题；宽度不足 480 pt 时隐藏缩放组，直接从「更多」使用缩放，不再通过溢出菜单嵌套访问。测试覆盖原生缩放、复制和菜单穿透操作，以及小 / 大窗口的工具栏状态。
- `./scripts/build-app.sh release` 成功生成 arm64 `dist/superuse.app`。Info.plist、完整严格签名检查、Bundle ID 和证书绑定 requirement 均通过；与已安装应用的 requirement 一致。未替换已安装应用。
- 使用复用 Pin 实现的独立原生预览检查了实际文字窗口和图片画布，使用专用剪贴板；外观对照图仅放在忽略的预览产物中。实机确认了文字选中后的复制反馈，以及最终窄窗口隐藏重复标题、保留复制 / 更多且不出现工具栏溢出按钮的外观。左侧紫色窗口捕获提示由 macOS 显示。界面工具随后多次返回窗口定位或 ScreenCaptureKit 采集错误，未完成实际拖动、边缘缩放、点击穿透、跨应用焦点、全屏 Space 和多显示器验证；自动测试结果不代替这些检查。

待实机检查：

1. 从截图及剪贴板分别 Pin 图片和文字，确认成功后返回原应用，创建 / 拖动不抢输入焦点，文字可选中复制。
2. 拖动浮窗与边缘缩放；滚动长图和长文本，检查图像比例、原始大小、复制像素及文字换行。
3. 调低不透明度，启用鼠标穿透，确认下层应用收到点击，再从菜单栏恢复操作。
4. 多个 Pin 的隐藏 / 显示与关闭；截图、滚动截图及取消前后检查原有可见状态，已有隐藏 Pin 不应自行显示。
5. 切换 Space 和其他应用的全屏窗口，跨不同缩放率显示器移动并断开外接显示器，检查置顶、坐标与窗口可达性。
6. 使用 VoiceOver、减少透明度和高对比度设置检查控制条与正文；退出应用后确认 Pin 不会恢复。

## 自动验证

- `swift test`：16 个核心测试与 20 个 AppKit 集成 / 布局测试全部通过。
- 登录项测试使用替身验证：打开设置不自动注册、开关注册 / 注销、初始 `.notFound` 时可注册、失败后恢复真实状态并允许重试、等待系统批准、从系统设置返回后刷新，以及区分登录启动和普通启动。测试不会改动本机登录项。
- “无法读取启动状态”排查：已安装 `/Applications/superuse.app` 的固定签名验证通过；系统 `backgroundtaskmanagementd` 日志明确记录该 App 的 `record not found`。原代码因 `.notFound` 禁用开关，修复后仅在用户打开开关时尝试注册，测试覆盖这一恢复路径。
- 截图交互测试覆盖桌面 / 全屏 / 重叠窗口命中、点击抖动与拖动阈值、鼠标松开后确认、窄选区拒绝、负坐标显示器、确认后冻结、旧快捷键偏好兼容、原位编辑布局与隔离剪贴板 PNG 导出。
- 操作栏定位回归覆盖 8 种选区位置 × 2 种滚动入口状态：反复展开 / 收起标注工具、复制及长文件名状态提示后，主操作栏和按钮保持原位；展开工具留在屏幕内且不与主操作栏重叠。亮色、暗色及两种高对比度布局也验证编辑按钮位置不变，并在空间充足时避开截图选区。上述检查使用测试图片和未显示的 AppKit 窗口，本次未执行真实截图的鼠标验收。
- `./scripts/build-app.sh release`：构建通过，生成 arm64 `dist/superuse.app`。
- `codesign --verify --deep --strict dist/superuse.app`：使用 `Suse Local Development` 证书的应用包签名验证通过。
- 修改临时应用副本的版本号并用同一证书重新签名：CDHash 变化，designated requirement 保持一致，且新副本通过原 requirement 验证。签名身份绑定 Bundle ID 与证书，不依赖构建内容哈希。
- 在无本机签名配置的临时目录执行打包脚本：缺少签名身份或显式指定 `SIGNING_IDENTITY=-` 均在构建前失败，不会退回临时签名。
- `plutil -lint dist/superuse.app/Contents/Info.plist`、`git diff --check`：通过。
- 布局测试覆盖亮色、暗色、高对比度亮色 / 暗色，在较小窗口尺寸检查设置页宽度、分组边界、剪贴板列表高度和截图工具栏边界。测试只覆盖窗口的 appearance，不修改系统外观设置。
- 工具条悬停与图标颜色调整后，完整回归和四种外观渲染通过。独立预览应用复用真实截图控制器、测试图片和隔离剪贴板，确认原生窗口中的红色取消 / 绿色完成图标及编辑展开。悬停使用 AppKit 原生 `showsBorderOnlyWhileMouseInside`；界面工具的坐标操作返回 `noWindowsAvailable`，尚未确认真实鼠标悬停高亮。
- 编辑按钮图文间距修复后，完整回归通过。布局测试检查四种外观下“编辑”和“完成标注”的图标到文字间距，并保留按钮外框位置不变的断言；新增检查在旧实现上失败。独立原生预览中确认两种状态的图文紧凑排列及切换后工具条位置稳定。本次未执行真实桌面截图或鼠标悬停验收。
- 截图编辑快捷键回归覆盖画布 / 按钮焦点下 Esc 取消且不改写剪贴板、⌘Z 撤销、⇧⌘Z 重做、图片像素恢复、空历史、新编辑使旧重做失效、修饰键匹配、退出编辑后不处理标注快捷键，以及文字输入 / 附加弹窗优先处理按键。独立原生预览中实际按下 ⌘Z、⇧⌘Z 和 Esc，确认标注数 1 → 0 → 1 及退出回调触发；使用测试图片和隔离剪贴板，未执行真实桌面截图。
- 更名后 `CFBundleName`、`CFBundleDisplayName`、可执行文件和应用包名均为 `superuse`；Bundle ID 仍为 `app.suse.mac`，签名 requirement 仍绑定原证书（本机证书指纹不公开）。
- 没有 SwiftUI import，没有第三方包依赖。

AppKit 集成测试使用专用 NSPasteboard、临时 UserDefaults suite 和临时目录，检查实际读写和图片像素。不会使用系统剪贴板作为测试数据源。

此前已通过 `dist/Suse.app` 实机进入截图覆盖层。本次界面重构与更名使用 `dist/superuse.app` 验证，未修改系统隐私设置或替换 `/Applications/Suse.app`。

## 原生界面

实机检查了原生工具箱、通用设置、统一快捷键页和剪贴板面板：分组背景清晰，控件对齐；剪贴板唤起后工具栏搜索框获得焦点，列表保持紧凑。更名后确认主窗口、设置窗口和菜单栏显示 superuse，截图默认快捷键仍为 ⇧⌘A。未修改或删除用户的剪贴板条目。

使用测试图片和隔离历史记录，渲染检查了设置、工具箱、剪贴板及原位预览 / 编辑操作栏的四种外观，图片位置保持不变。离屏 `cacheDisplay` 无法完整呈现窗口服务器合成的玻璃材质和 vibrant 选中态，不能用它代替实机材质检查。可重新生成布局预览：

```sh
SUSE_UI_PREVIEW_DIRECTORY="$PWD/.build/ui-previews" swift test --filter InterfaceLayoutTests
```

实机后续操作多次遇到界面控制工具的窗口状态变化提示；完整的快捷键、滚动截图、全屏 Space、减少透明度 / 动态效果与 VoiceOver 验收仍保留在下方清单。此前已确认截图入口能显示冻结画面和自动窗口选框。

1. 启动 `dist/superuse.app`，确认菜单栏入口可用，关闭窗口后快捷键仍可唤起。
2. 在快捷键页录入组合、取消录入、停用一项；尝试给两个命令设置同一组合，应显示冲突且保留原设置。
3. 在文本编辑器复制两段不同文字和一张图片，按需允许 macOS 的剪贴板读取提示；打开历史搜索，用 ↑↓ / Return 复制，⌘E 编辑，再次打开确认内容更新。将系统访问设为拒绝时面板应显示权限状态。
4. 将焦点放在文本编辑器输入框，用 ⌃⌥V → ↓ → ⌘Return，确认内容输入原位置。拒绝辅助功能权限时应只复制并提示，不向其他应用发送按键。
5. 在剪贴板设置中开启持久化，退出并重启，确认恢复；关闭持久化后确认本机文件删除。测试使用不含秘密的内容。
6. 在每块显示器分别按 ⇧⌘A：鼠标在桌面和全屏应用上时框选全屏，在普通窗口上时选中最前面的窗口。单击松开后确认；在任意目标上正向或反向拖动均变为区域。确认后鼠标移动不再改变选区，Esc 退出。
7. 确认截图后应直接显示标注工具并可在原位绘制。添加箭头、文字、实心遮挡及马赛克打码，检查不同颗粒大小、正反向框选和导出范围，叠加打码应保留已有遮挡；有标注时滚动截图不可用，撤销或清除全部标注后应恢复，重做后再次禁用。用 ⇧⌘C 复制并保留预览、Enter 复制并结束、⌘S 保存 PNG。检查像素尺寸、方向、标注位置以及重选行为。
8. 在长网页或文档中框选内容区，从操作栏进入滚动截图。确认选区边框保留且滚动事件传入目标应用；完成按钮和同一截图快捷键都能返回原位长图预览。检查接缝、静止不重复、滚动过快提示、取消返回原图、达到限制可保存已有部分，并确认控制面板和选框不进入成品。
9. 检查亮色 / 暗色、减少透明度、VoiceOver 标签以及截图权限拒绝后的重试。
10. 从固定安装路径的 `superuse.app` 打开“设置 → 通用”，切换“开机启动”并核对系统登录项。若需要批准，通过页面按钮进入系统设置，允许后返回应用确认开关更新。登录启动应只显示菜单栏入口，手动启动仍打开工具箱；关闭开关后不再自动启动。实际注册、注销与重新登录验收尚未执行，本次未更改用户的登录项配置。

取色放大镜：分别在全屏、窗口和拖动框选时移动鼠标，检查中心像素、坐标、边缘换位和颜色值；全屏应保持原亮度，四周边框清晰可见。逐次按 ⇧ 应循环 RGB／HEX／HSL，按住不重复切换；⌘C 仅复制当前格式色值并保留选区。跨 Retina 与非 Retina 显示器检查坐标和颜色格式，确认选区后浮层消失，编辑与文本输入快捷键不受影响。浮层不显示“全屏／窗口／区域 · 确认”行。

滚动截图当前限定为用户手动向下滚动；固定元素、重复图案和动画可能需要重新选区或回退重试。区域截图一次只跨一块显示器。
